{
  "name": "wordcamp-saopaulo",
  "version": "1.0.0",
  "description": "[View the documentation](#)",
  "scripts": {
    "build": "node-sass --output-style compressed --source-map true -o ./ assets/sass",
    "watch": "onchange \"assets/**/*.scss\" -- npm run build"
  },
  "devDependencies": {
    "node-sass": "^4.13.1",
    "onchange": "^6.0.0"
  }
}
